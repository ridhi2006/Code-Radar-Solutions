#include <stdio.h>
int main(){
    char ch;
    if(ch>='A'' && ch<='Z'){
        printf("Uppercase",alphabet);
    }
    else if(ch>='a' && ch<='z'){
        printf("Lowercase",alphabet);
    }
    else{
        printf("Not an alphabet",alphabet);
    }
    return 0;
}
